# example MongoDB with Mongo Express

# pull mongo:3.5
# docker run --name some-mongo -d mongo:3.5
# docker run --link some-mongo:mongo -p 8081:8081 mongo-express
# docker exec -it some-mongo mongo admin